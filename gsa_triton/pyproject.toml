[project]
name = "gsa-triton"
version = "0.1.0"
description = "GSA reference (PyTorch) and Triton-optimized implementations with benchmark"
requires-python = ">=3.11,<3.13"
dependencies = [
    "torch>=2.4.0",
    "triton>=3.0.0",
]

